<p id="notice"><%= notice %></p>

  <h1>Artists</h1>

  <p>
      <%= link_to new_artist_path do %>
      <i class="glyphicon glyphicon-plus"> Add</i>
  <% end %>
  </p>

      <div class="row">
          <% @artists.each do |artist| %>
       <div class="col-md-6">
        <div class="well">
          <h2><%= link_to artist.name, artist %></h2>
          <h4><%= link_to artist %></h4>
          <h4><%= link_to edit_artist_path(artist) %>
           <i class="glyphicon glyphicon-pencil">
          </i></h4>
          <p>
          <%= link_to 'Destroy', artist, method: :delete, data: { confirm: 'Are you sure?' } %>  <i class="glyphicon glyphicon-trash"></i>
          <% end %>
         <!--  <i class="glyphicon glyphicon-star-empty"></i> -->
          </p>
       <!--   <%= link_to edit_song_path(song) %> -->
        </div>
    </div>
</div>
  


